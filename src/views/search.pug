extends base 
include mixins/video

block content 
  form(method="GET")
    input(type="text",name="keyword",required,placeholder="Enter the keyword")
    input(type="submit",value="Search")
  
  each video in videos
    +video(video)