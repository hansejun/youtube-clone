extends ../base 
include ../mixins/video

block content 
  img(src=`${user.avatarUrl}` ,width="50",height="50")
  h3 Name #{user.name}
  h3 Email #{user.email}
  hr 
  a(href=`/users/${loggedInUser._id}/edit`) Edit Profile &rarr;
  br
  a(href=`/users/${loggedInUser._id}/edit-password`) Changed Password &rarr;